import { AuthShopInterceptor } from './auth-shop.interceptor';

describe('AuthShopInterceptor', () => {
  it('should be defined', () => {
    expect(new AuthShopInterceptor()).toBeDefined();
  });
});
