import { ProductInterceptorInterceptor } from './product-interceptor.interceptor';

describe('ProductInterceptorInterceptor', () => {
  it('should be defined', () => {
    expect(new ProductInterceptorInterceptor()).toBeDefined();
  });
});
